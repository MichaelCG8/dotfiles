# These commmands were run to get .tmux.conf the first time, but now they are version controlled.
# tmux.conf.local is the one I change. I'm not meant to touch tmux.conf.
# wget --output-document tmux.conf.symlink https://raw.githubusercontent.com/gpakosz/.tmux/master/.tmux.conf
# wget --output-document tmux.conf.local.symlink https://raw.githubusercontent.com/gpakosz/.tmux/master/.tmux.conf.local
